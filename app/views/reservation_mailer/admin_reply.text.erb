========================
<%= "#{@reservation.name} 様" %>
========================

この度は、ご予約ありがとうございました。
<%= "#{@reservation.email} に送信" %>

この度は、ご予約ありがとうございました。
ご予約内容は以下の通りです。

【ご予約内容】
<%= "映画館: #{@theater.name}" %>
<%= "映画作品: #{@movie.name}" %>
<%= "上映開始時間: #{@schedule.start_time}" %>
<%= "上映終了時間: #{@schedule.end_time}" %>
<%= "座席: #{@sheet.row} - #{@sheet.column}" %>
